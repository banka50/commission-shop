# Комиссионный магазин

Веб-приложение для автоматизации работы комиссионного магазина. Построено на Flask + SQLAlchemy.

## Установка и запуск

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cd src
python app.py
```

Приложение запустится на `http://127.0.0.1:5000`.

Настройка БД — в `src/config.toml` или `src/.env`. По умолчанию используется PostgreSQL.

### Конфигурация

Настройки задаются тремя способами (приоритет от высшего к низшему):

1. **Переменные окружения** — заданные в терминале или системе
2. **`src/.env`** — файл с переменными окружения
3. **`src/config.toml`** — файл конфигурации

Ключевые переменные:

| Переменная | Описание | По умолчанию |
|---|---|---|
| `DB_ENGINE` | СУБД: `postgresql`, `sqlite`, `mysql` | `postgresql` |
| `POSTGRES_HOST` | Хост PostgreSQL | `127.0.0.1` |
| `POSTGRES_PORT` | Порт PostgreSQL | `5432` |
| `POSTGRES_DB` | Имя базы данных | `db` |
| `POSTGRES_USER` | Пользователь PostgreSQL | `postgres` |
| `POSTGRES_PASSWORD` | Пароль PostgreSQL | `root` |
| `SQLITE_PATH` | Путь к файлу SQLite | `commission_shop.db` |
| `SECRET_KEY` | Секретный ключ Flask | `supersecretkey` |

Для быстрого старта без сервера БД — используйте SQLite:
```bash
# src/.env
DB_ENGINE=sqlite
```

## Workflow

### 1. Регистрация комитента

Прежде чем принять товар, нужно зарегистрировать комитента — человека, который сдаёт вещи на реализацию.

**Комитенты → Добавить комитента** — заполнить ФИО, контакты, паспортные данные, ИНН.

---

### 2. Приёмка товара

Когда комитент приносит вещи:

1. **Акты приёма → Добавить акт приёма** — указать номер акта, дату, комитента и условия комиссии (процент и минимальную сумму).
2. Открыть созданный акт → нажать **«Добавить товар»** — ввести наименование, описание, цену, даты, при необходимости загрузить фотографии.
3. Повторить для каждого принесённого товара.

Все товары акта видны прямо на его странице. Каждый товар получает статус **«На витрине»**.

---

### 3. Оформление продажи

Когда товар продан:

1. Открыть нужный товар (**Товары** → выбрать или со страницы акта приёма).
2. Нажать **«Оформить продажу»** — форма откроется с уже выбранным товаром.
3. Заполнить дату, цену продажи, сумму комиссии и статус (*Ожидает оплаты / Оплачено*).

После оформления продажи товар переходит в статус **«Продан»**.

> Кнопка «Оформить продажу» доступна только для товаров со статусом **«На витрине»**.

---

### 4. Возврат от покупателя

Если покупатель вернул товар в магазин:

1. На странице товара нажать **«Оформить возврат от покупателя»**.
2. Заполнить дату и выбрать статус **«Возврат от покупателя»**.
3. Товар автоматически вернётся в статус **«На витрине»** и снова доступен для продажи.

Вся история продаж по каждому товару хранится и видна на его странице.

---

### 5. Возврат товара комитенту

Если товар не продался и магазин возвращает его комитенту:

1. **Акты возврата → Создать акт возврата** — указать номер, дату, комитента.
2. На странице акта нажать **«Добавить»** и выбрать товар из списка доступных.
3. Товар переходит в статус **«Возвращён комитенту»** и больше не может быть продан.

При удалении акта возврата все включённые в него товары возвращаются в статус **«На витрине»**.

---

### 6. Отчёты по продажам

Для формирования периодической отчётности:

**Отчёты по продажам → Добавить отчёт** — выбрать тип (*Ежедневный / Еженедельный / Ежемесячный / Квартальный / Годовой*), указать дату и описание.

---

## Структура данных

```
Consignor (комитент)
    ├── ConsignorReport (акт приёма)
    │       └── Product (товар) ── Sale (продажа, может быть несколько)
    └── ConsignorReturn (акт возврата)
            └── Product (возвращённые товары)

SalesReport (отчёт по продажам) — самостоятельный документ
```

### Consignor — комитент
| Поле | Тип | Описание |
|---|---|---|
| last_name, first_name, middle_name | varchar | ФИО |
| email | varchar | Уникальный email |
| phone_number | varchar | Уникальный номер телефона |
| passport_data | varchar | Паспортные данные (уникальные) |
| inn | varchar(12) | ИНН (уникальный) |

### ConsignorReport — акт приёма товара
| Поле | Тип | Описание |
|---|---|---|
| number | varchar | Уникальный номер акта |
| date | date | Дата приёма |
| description | varchar | Описание |
| consignor_id | FK | Комитент, сдавший товар |
| commission_pct | numeric(5,2) | Процент комиссии магазина |
| commission_min | numeric(10,2) | Минимальная сумма комиссии, ₽ |

### ConsignorReturn — акт возврата товара комитенту
| Поле | Тип | Описание |
|---|---|---|
| number | varchar | Уникальный номер акта |
| date | date | Дата возврата |
| description | varchar | Примечание (необязательно) |
| consignor_id | FK | Комитент, которому возвращают товар |

### Product — товар
| Поле | Тип | Описание |
|---|---|---|
| product_name | varchar | Наименование |
| description | varchar | Описание (необязательно) |
| delivery_date | date | Дата приёма на реализацию |
| expiry_date | date | Срок реализации |
| price | numeric(10,2) | Цена |
| status | enum | `На витрине` / `Продан` / `Возвращён комитенту` |
| consignor_report_id | FK | Акт приёма, по которому поступил товар |
| consignor_return_id | FK | Акт возврата (заполняется при возврате комитенту) |

### Sale — продажа
| Поле | Тип | Описание |
|---|---|---|
| product_id | FK | Товар, который был продан |
| sale_date | date | Дата продажи |
| sale_price | numeric(10,2) | Цена продажи |
| commission | numeric(10,2) | Итоговая сумма комиссии: max(цена × % / 100, минимум) |
| status | enum | `Ожидает оплаты` / `Оплачено` / `Возврат от покупателя` |

### SalesReport — отчёт по продажам
| Поле | Тип | Описание |
|---|---|---|
| number | varchar | Уникальный номер отчёта |
| date | date | Дата отчёта |
| report_type | enum | `Ежедневный` / `Еженедельный` / `Ежемесячный` / `Квартальный` / `Годовой` |
| description | varchar | Описание (необязательно) |
